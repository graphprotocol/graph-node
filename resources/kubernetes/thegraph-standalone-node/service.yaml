apiVersion: v1
kind: Service
metadata:
  name: thegraph-standalone-node
spec:
  type: LoadBalancer
  selector:
    app: thegraph-standalone-node
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
