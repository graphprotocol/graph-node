apiVersion: v1
kind: Service
metadata:
  name: thegraph-local-node
spec:
  type: LoadBalancer
  selector:
    app: thegraph-local-node
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 8000
