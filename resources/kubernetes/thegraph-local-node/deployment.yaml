apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: thegraph-local-node
spec:
  selector:
    matchLabels:
      app: thegraph-local-node
  replicas: 1
  template:
    metadata:
      labels:
        app: thegraph-local-node
    spec:
      containers:
        - name: thegraph-local-node
          image: gcr.io/the-graph-staging/thegraph-local-node:latest
          ports:
            - name: http
              containerPort: 8000
          env:
            - name: THEGRAPH_SENTRY_URL
              valueFrom:
                secretKeyRef:
                  name: thegraph-sentry-url
                  key: value
