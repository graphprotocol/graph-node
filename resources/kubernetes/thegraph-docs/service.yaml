apiVersion: v1
kind: Service
metadata:
  name: thegraph-docs
spec:
  type: LoadBalancer
  selector:
    app: thegraph-docs
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
