#! /bin/bash

if [ $# != 1 ]; then
    echo "usage: create <name>"
    exit 1
fi

api="http://127.0.0.1:8020/"

data=$(printf '{"jsonrpc": "2.0", "method": "subgraph_pause", "params": {"name":"%s"}, "id":"1"}' "$1")
curl -s -H "content-type: application/json" --data "$data" "$api"
