apiVersion: v1
kind: Service
metadata:
  name: thegraph-node
spec:
  type: LoadBalancer
  selector:
    app: thegraph-node
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
