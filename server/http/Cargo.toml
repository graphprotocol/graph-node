[package]
name = "graph-server-http"
version = "0.22.0"
edition = "2018"

[dependencies]
futures = "0.1.21"
graphql-parser = "0.3"
http = "0.2"
hyper = "0.13"
serde = "1.0"
graph = { path = "../../graph" }
graph-graphql = { path = "../../graphql" }

[dev-dependencies]
graph-mock = { path = "../../mock" }
