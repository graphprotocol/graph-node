[package]
name = "graph-server-http"
version = "0.1.0"

[dependencies]
futures = "0.1.21"
graphql-parser = "0.2.0"
http = "0.1.5"
hyper = "0.12.7"
serde = "1.0"
graph = { path = "../../graph" }
graph-graphql = { path = "../../graphql" }

[dev-dependencies]
tokio-executor = "0.1.2"
