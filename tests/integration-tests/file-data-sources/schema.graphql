type IpfsFile @entity {
  id: ID!
  content: String!
}

type IpfsFile1 @entity {
  id: ID!
  content: String!
}

type Holder @entity {
  id: ID!
  tokens: Token! @derivedFrom(field: "holder")
}

type Token @entity {
  id: ID!
  holder: Holder!
  color: String
}