{
  "name": "timestamp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "t": "pnpm bin graph",
    "codegen": "graph codegen --skip-migrations",
    "create:test": "graph create test/timestamp --node $GRAPH_NODE_ADMIN_URI",
    "deploy:test": "graph deploy test/timestamp --version-label v0.0.1 --ipfs $IPFS_URI --node $GRAPH_NODE_ADMIN_URI"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.69.0",
    "@graphprotocol/graph-ts": "0.34.0"
  }
}
