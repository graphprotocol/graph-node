initSidebarItems({"struct":[["active","If there are multiple entries for the same IPFS hash (`subgraph`) only one of them will be active. That’s the one we use for querying"],["created_at",""],["id",""],["name",""],["network",""],["shard",""],["star","Represents `table_name.*`, which is sometimes needed for efficient count queries. It cannot be used in place of `all_columns`, and has a `SqlType` of `()` to prevent it being used that way"],["subgraph",""],["version","The subgraph layout scheme used for this subgraph"]]});