initSidebarItems({"struct":[["BIN_SIZE",""],["Bin","One bin of durations. The bin starts at time `start`, and we’ve added `count` entries to it whose durations add up to `duration`"],["MovingStats","Collect statistics over a moving window of size `window_size`. To keep the amount of memory needed to store the values inside the window constant, values are put into bins of size `bin_size`. For example, using a `window_size` of 5 minutes and a bin size of one second would use 300 bins. Each bin has constant size"],["WINDOW_SIZE",""]]});